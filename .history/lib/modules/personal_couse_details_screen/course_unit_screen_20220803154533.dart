import 'package:flutter/material.dart';

class CourseUnitScreen extends StatelessWidget {
  const CourseUnitScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}